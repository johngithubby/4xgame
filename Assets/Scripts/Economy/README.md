# Economy Scripts

## Files

- `ResourceWallet.cs`: Adds and spends local coin resources on a `SaveGameData` instance.

## Compile

Unity compiles this file as part of the `LaneSurvivor.Runtime` assembly.

## Behavior

The wallet exposes small deterministic helpers so base-building UI and tests can share the same resource rules.
