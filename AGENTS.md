# AGENTS

## Instructions

- When creating or modifying code, add comments for nearly all non-trivial lines and logic; only truly trivial lines should remain uncommented.
- After making code changes, compile (when applicable) and run tests.
- Whenever this file is modified, apply the same changes to the repo's `.codex/AGENTS.md`.
- If you modify code in a file and the directory containing that file has no README, create a README that documents: each source file's purpose, how to compile (if applicable), and what the files do and how.
- After completing a code change, update the directory README as needed so it reflects the latest modifications.
- For any visual, art, model, animation, UI, or layout change, always verify visually before reporting completion: capture a screenshot or render of the actual result, inspect it against the requested reference/model or intended design, and report what matched or did not match. Tests and structural assertions are not enough for these tasks.
